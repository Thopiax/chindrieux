# Supabase project connection. Copy to .env.local and fill in.
# These values are public, not secrets: the anon key is meant to ship in the
# client, and everyone with the app link reads and writes the same shared data.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
